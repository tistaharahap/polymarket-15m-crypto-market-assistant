# Trend Sizing (TSZ) tuning
# Client-side env vars must be prefixed with NEXT_PUBLIC_

# Poll conditional balances (shares + allowance) for active Up/Down tokens while trading is active.
NEXT_PUBLIC_TSZ_BALANCE_POLL_MS=1500

# Force-refresh balance if cached value is older than this.
NEXT_PUBLIC_TSZ_BALANCE_STALE_MS=5000
